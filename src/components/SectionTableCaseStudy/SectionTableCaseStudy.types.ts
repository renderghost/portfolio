export interface SectionTableCaseStudyProps {
  affiliation: string;
  affiliationURL: string;
  role: string;
  endDate: string;
  type: string;
  usecase?: 'full' | 'large' | 'default' | 'small';
}
